import 'package:flutter/material.dart';

class IdCardViewMobile extends StatelessWidget {
  const IdCardViewMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
