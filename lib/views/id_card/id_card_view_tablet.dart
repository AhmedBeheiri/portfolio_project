import 'package:flutter/material.dart';

class IdCardViewTablet extends StatelessWidget {
  const IdCardViewTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
