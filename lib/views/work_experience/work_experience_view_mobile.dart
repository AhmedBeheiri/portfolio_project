import 'package:flutter/material.dart';

class WorkExperienceViewMobile extends StatelessWidget {
  const WorkExperienceViewMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
