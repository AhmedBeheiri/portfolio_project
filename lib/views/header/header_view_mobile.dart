import 'package:flutter/material.dart';

class HeaderViewMobile extends StatelessWidget {
  const HeaderViewMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
