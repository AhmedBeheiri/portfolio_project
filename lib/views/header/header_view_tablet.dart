import 'package:flutter/material.dart';

class HeaderViewTablet extends StatelessWidget {
  const HeaderViewTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
